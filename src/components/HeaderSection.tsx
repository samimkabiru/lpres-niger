import HeroSection from './HeroSection';
import NavBar from './NavBar';

const HeaderSection = () => {
  return (
    <header>
      <NavBar />
      <HeroSection />
    </header>
  );
};

export default HeaderSection;
